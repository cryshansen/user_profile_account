These folders are intended for display junctionality purposes only for the image gallery masonry layout. 

It is much better to configure the system via the drupal file system mechanisms for image module coupled with the responsive module load via some means like a migration load for creating user specific galleries through content. 

Once this is achieved, the code should pull from the users account folder. 

